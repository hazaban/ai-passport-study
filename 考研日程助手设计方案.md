# 考研日程助手 · 设计方案

> 基于 FoloToy AI 通行证（ESP32-C3 + 240×320 彩屏 + ES8311 音频 + 三按键）的备考日程与提醒工具

---

## 一、整体架构

```
┌─────────────────────────────────────────────────────┐
│                    主菜单（新增一项）                  │
│                   「考研助手」                        │
└──────────────────────────┬──────────────────────────┘
                           │
              ┌────────────▼────────────┐
              │     今日 Todo 首页       │  ← 启动默认页
              │  (今日任务列表 + 进度)    │
              └────┬───────┬───────┬────┘
                   │       │       │
          长按OK返回  上下选择  短按OK进入
                   │       │       │
         ┌─────────▼──┐ ┌──▼───────▼──────────┐
         │ 任务详情页  │ │   添加任务页         │
         │ (勾选/详情) │ │ (类别选择 + 语音输入) │
         └────────────┘ └─────────────────────┘
                            │
                  ┌─────────▼─────────┐
                  │  类别选择（10 类）  │
                  └───────────────────┘
```

### 技术栈
- **UI 框架**：LVGL（沿用项目现有封装 `ui_pixel` + 自定义组件）
- **存储层**：NVS（任务/配置，结构化 KV）+ SPIFFS（语音包音频文件）
- **音频播放**：ES8311 I2S + ADPCM 压缩解码（节省 Flash）
- **提醒调度**：FreeRTOS 软件定时器 + 低功耗定时器（深睡唤醒）
- **语音输入**：本地录音（PCM 16kHz）→ BLE 转发手机端 STT（推荐方案，见 §5.3）
- **语音包**：SPIFFS 目录式管理，按角色名分目录

---

## 二、任务类别体系

共 10 个类别，每个类别有独立的**颜色标识**和**完成鼓励文案**。

| 编号 | 类别 | 主题色 | 完成鼓励音文案（水豚噜噜语气） |
|------|------|--------|-------------------------------|
| 1 | 日常秩序 | 🟢 草绿 `#82BE2D` | 「生活节奏稳稳的，学习才能更高效哦～」 |
| 2 | 高等数学 | 🔴 红 `#E43B2F` | 「数学又攻克一关！你的思维越来越清晰啦，继续冲！」 |
| 3 | 线性代数 | 🟠 橙 `#FFB23E` | 「矩阵的世界你又前进了一步，向量空间等你征服～」 |
| 4 | 概率论 | 🟡 黄 `#FFD928` | 「概率题搞定了！运气也是实力的一部分，但你靠的是真本事！」 |
| 5 | 数据结构 | 🔵 蓝 `#1689E8` | 「链表树图全拿捏，算法小能手就是你！」 |
| 6 | 计算机组成原理 | 🟣 紫 `#9B59B6` | 「CPU 内存总线都搞懂了，你就是行走的计算机组成书！」 |
| 7 | 操作系统 | 🟤 棕 `#8B4513` | 「进程调度内存管理通关，OS 大师指日可待！」 |
| 8 | 计算机网络 | 🌊 青 `#1ABC9C` | 「七层模型了然于胸，网络世界任你遨游～」 |
| 9 | 英语 | 💗 粉 `#FF6B9D` | 「单词量 +N！坚持下去，阅读速度会越来越快的！」 |
| 10 | 政治 | ❤️ 深红 `#C0392B` | 「政治知识点又巩固了，马原毛中特全都不在话下！」 |

---

## 三、核心功能设计

### 3.1 今日 Todo 首页

**界面布局**（240×320 竖屏）：

```
┌─────────────────────────┐
│ 12月25日 周三  ☀️ 进度 3/8 │  ← 顶部状态栏
├─────────────────────────┤
│ 🟩 日常秩序  07:00       │
│ [✓] 早上洗漱             │  ← 已完成项灰色+划线
│                         │
│ 🟥 高等数学  08:00       │
│ [ ] 高数第三章习题        │  ← 当前选中项高亮
│                         │
│ 🟦 数据结构  10:30       │
│ [ ] 链表专题复习          │
│                         │
│ ...                     │
│                         │
│ [+ 添加任务]  [设置]      │  ← 底部操作区
└─────────────────────────┘
```

**交互**：
- 上/下键：滚动任务列表
- 短按 OK：进入任务详情（可勾选完成 / 编辑 / 删除）
- 长按 OK：返回主菜单
- 在「+ 添加任务」上按 OK：进入添加任务流程
- 在「设置」上按 OK：进入设置页

**自动排序规则**：按设定时间升序，未设时间的任务排在末尾。

### 3.2 日常秩序 · 固定场景提醒

这是「日常秩序」类别的核心，预设以下场景，每个场景都有**专属触发动作**和**语音提示**：

| 场景 | 触发方式 | 语音提示文案（水豚噜噜） | 附加行为 |
|------|---------|------------------------|---------|
| 🌅 早上洗漱 | 勾选"早上洗漱"任务完成时 | 「开启新的一天，要信心满满，踏实走好每一步哦～」 | 播放轻快提示音（RTTTL 旋律） |
| 📚 开始学习 | 勾选"早饭"完成后自动触发 / 手动点"开始学习" | 「学习时间到啦！请把手机放到另一个房间，保持专注，你可以的！」 | 启动专注模式（屏蔽非紧急提醒） |
| 🍱 午饭时间 | 勾选"午饭"任务完成时 | 「午饭吃得香，下午才有劲～饭后可以背一会儿单词哦，建议午休 30 分钟，养足精神再战！」 | 30 分钟后自动提醒午休结束 |
| 🍜 晚饭时间 | 勾选"晚饭"任务完成时 | 「晚饭时间到！劳逸结合，吃完出去散散步，回来继续加油～」 | — |
| 🛁 睡前洗漱 | 勾选"睡前洗漱"完成时 | 「洗漱完毕，一天的辛苦结束啦，准备好休息了吗？」 | — |
| 😴 睡觉（专属按钮） | 点击"睡觉"按钮 / 勾选"睡觉"任务 | 「睡觉啦～先回顾一下今天完成了哪些任务，是不是很有成就感？请把手机放到另一个房间，好的睡眠才是新一天的开始哦，晚安～」 | 显示今日完成总结页 → 进入夜间息屏模式 |
| 💇 洗头发提醒 | 按设定周期（如每 2 天）自动提醒 | 「该洗头发啦～清清爽爽，学习效率更高！」 | — |

> **注**：所有固定场景的文案和语音均可在设置中自定义替换。

### 3.3 自定义提醒

用户可添加任意自定义提醒，支持：
- **提醒名称**（文字输入，最多 20 字）
- **提醒时间**（单次 / 每天 / 每周几 / 间隔天数）
- **关联类别**（选择 10 类之一，决定完成音和颜色）
- **是否语音播报**（开关）

### 3.4 任务完成提示音

每个科目完成时播放 **"短音效 + 鼓励语"** 的组合：
1. 先播放 1-2 秒的科目专属 RTTTL 旋律（轻快、辨识度高）
2. 接着播放该科目的鼓励语音文案
3. 屏幕上显示对应科目的彩色动画 + 文字

**各科专属旋律风格**（RTTTL）：
| 科目 | 旋律风格 |
|------|---------|
| 日常秩序 | 温暖上行琶音（C→E→G→C） |
| 高等数学 | 激昂大调（类似胜利号角） |
| 线性代数 | 简洁明快的音阶跑动 |
| 概率论 | 轻快跳跃的断奏 |
| 数据结构 | 科技感电子音 |
| 计算机组成原理 | 沉稳有力的低频节奏 |
| 操作系统 | 复杂多层的和声（模拟进程调度） |
| 计算机网络 | 波动起伏的波浪音 |
| 英语 | 轻柔优美的旋律 |
| 政治 | 庄严稳重的进行曲风 |

---

## 四、语音包系统

### 4.1 设计思路

由于 ESP32-C3 算力有限，**不做端侧 TTS**，采用**预录制语音包**方案。语音以 ADPCM 压缩格式存储在 SPIFFS 中，播放时实时解码。

### 4.2 语音包结构

```
/spiffs/voice_packs/
├── capybara_lulu/              # 水豚噜噜（默认）
│   ├── pack_info.json          # 角色名、作者、版本
│   ├── morning_wash.adpcm      # 早上洗漱完成语
│   ├── start_study.adpcm       # 开始学习提醒
│   ├── lunch.adpcm             # 午饭提醒
│   ├── dinner.adpcm            # 晚饭提醒
│   ├── night_wash.adpcm        # 睡前洗漱
│   ├── sleep.adpcm             # 睡觉提醒
│   ├── hair_wash.adpcm        # 洗头发提醒
│   ├── complete_daily.adpcm    # 日常秩序完成
│   ├── complete_math.adpcm     # 高数完成
│   ├── complete_linear.adpcm   # 线代完成
│   ├── complete_prob.adpcm     # 概率论完成
│   ├── complete_ds.adpcm       # 数据结构完成
│   ├── complete_co.adpcm       # 计组完成
│   ├── complete_os.adpcm       # 操作系统完成
│   ├── complete_network.adpcm  # 计网完成
│   ├── complete_english.adpcm  # 英语完成
│   ├── complete_politics.adpcm # 政治完成
│   └── custom_1.adpcm ~ custom_10.adpcm  # 自定义提醒预留
└── other_character/            # 其他角色
    └── ...
```

### 4.3 语音包管理

- **默认内置**：水豚噜噜（capybara_lulu）
- **更换方式**：通过 BLE 从手机端下载新语音包 → 写入 SPIFFS → 切换生效
- **设置页**：显示已安装的语音包列表，上下键选择切换
- **内存占用估算**：每条语音约 2-3 秒，ADPCM 4:1 压缩，16kHz 采样 → 每条约 8-12KB，共约 20 条 → 总计约 200KB，8MB Flash 完全够用

### 4.4 ADPCM 解码

使用 IMA-ADPCM 算法，纯软件解码，每帧解码后送入 I2S 播放。参考现有 `bsp_audio` 的 I2S 写入接口。

---

## 五、语音输入添加任务

### 5.1 方案选型对比

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **A. 本地录音 + 手机端 STT**（推荐） | 通行证录音 → BLE 传手机 → 手机端 STT 转文字 → 回传 | 不依赖云端、免费、响应快 | 需要手机配合 |
| B. 本地录音 + 云端 STT | 录音 → Wi-Fi 上传云端 STT | 无需手机APP | 依赖网络、有成本、隐私问题 |
| C. 纯文字输入 | 三按键选字 | 无需音频 | 操作繁琐、效率低 |

### 5.2 推荐方案详细流程（方案 A）

```
用户在添加任务页选择"语音输入"
        │
        ▼
  开始录音（屏幕显示"正在听..."动画）
        │
        ▼
  录音完成（自动检测静音结束 / 手动停止）
        │
        ▼
  检查 BLE 连接状态
     ├── 已连接 → 通过 BLE GATT 发送音频数据
     └── 未连接 → 显示"请连接手机"提示
        │
        ▼
  手机端 STT 识别 → 返回文字
        │
        ▼
  通行证显示识别结果，用户确认
        │
        ▼
  选择类别 → 保存任务
```

**录音规格**：16kHz / 16bit / 单声道，最长 10 秒（约 320KB，内存可承受）

### 5.3 降级方案

如果不连手机，提供**快速文字输入**：
- 预置常用任务模板（"做高数题""背单词""复习数据结构"等）
- 上下键选择模板，OK 确认
- 可微调时间

---

## 六、提醒调度系统

### 6.1 调度架构

```
┌──────────────────────────────────┐
│        调度器（FreeRTOS 任务）     │
│  - 维护下一个提醒的定时器          │
│  - 到点触发：响铃 + 弹窗 + 震动    │
│  - 每日 00:00 刷新今日任务列表     │
└──────────────┬───────────────────┘
               │
    ┌──────────▼──────────┐
    │   提醒队列（按时间排序） │
    │  - 起床 07:00        │
    │  - 高数 08:00        │
    │  - 午饭 12:00        │
    │  - ...               │
    └──────────────────────┘
```

### 6.2 低功耗设计

- **正常运行**：FreeRTOS 定时器 + LVGL 刷新
- **息屏待机**：关闭背光 + 降低刷新率，提醒到点自动亮屏
- **夜间模式**：点击睡觉按钮后进入深度休眠，仅 RTC 定时器运行，早上设定时间唤醒

### 6.3 提醒触发方式

到点时同时触发：
1. **屏幕亮屏** + 显示提醒内容（任务名 + 类别颜色）
2. **播放提示音**（RTTTL 旋律 + 语音播报）
3. **任务标记为"待完成"**，在列表中置顶闪烁

**提醒重复**：如果 3 分钟内未按 OK 确认，间隔 3 分钟再响一次，最多响 3 次。

---

## 七、数据存储设计

### 7.1 NVS 存储结构

使用 ESP-IDF 的 NVS（非易失性存储），按命名空间分区：

| 命名空间 | Key | 内容 | 大小估算 |
|---------|-----|------|---------|
| `tasks` | `task_<id>` | 单条任务（JSON） | 每条 ~200B |
| `tasks` | `task_count` | 任务总数 | 4B |
| `tasks` | `next_id` | 下一个任务 ID | 4B |
| `config` | `voice_pack` | 当前语音包名 | 32B |
| `config` | `wake_time` | 起床时间 | 2B |
| `config` | `sleep_time` | 睡觉时间 | 2B |
| `config` | `volume` | 音量 0-100 | 1B |
| `stats` | `yyyy_mm_dd` | 当日完成统计 | ~30B |

**任务数据结构**（JSON）：
```json
{
  "id": 1,
  "title": "高数第三章习题",
  "category": 2,           // 类别编号 0-9
  "time": "08:00",         // 提醒时间，可为空
  "repeat": 0,             // 0=单次 1=每天 2=工作日 3=自定义
  "done": false,           // 今日是否完成
  "done_at": 0,            // 完成时间戳
  "created_at": 1701234567
}
```

### 7.2 统计数据

记录每日完成情况，在「今日总结」（睡觉按钮触发时）和「设置-学习统计」中展示：
- 今日完成 / 总任务数
- 各科完成数量
- 连续打卡天数
- 本周学习时长估算

---

## 八、页面导航与按键映射

### 8.1 页面清单

| 页面 | 进入方式 | 上键 | 下键 | 短按OK | 长按OK |
|------|---------|------|------|--------|--------|
| 主菜单 | 开机 | 上移 | 下移 | 进入选中项 | — |
| 今日Todo | 主菜单进入 | 上移任务 | 下移任务 | 进入选中任务详情 / 操作按钮 | 返回主菜单 |
| 任务详情 | Todo 列表选中任务 | — | — | 勾选完成 / 取消 | 返回 Todo |
| 添加任务 | Todo 页底部「+」 | 切换输入方式 | 切换输入方式 | 确认添加 | 返回 Todo |
| 类别选择 | 添加任务时选类别 | 上移类别 | 下移类别 | 选中类别 | 返回上一页 |
| 语音输入 | 添加任务选"语音输入" | — | — | 开始/停止录音 | 返回上一页 |
| 提醒弹窗 | 到点自动弹出 | — | — | 确认（标记完成） | 稍后提醒 |
| 睡觉总结页 | 点击睡觉按钮 | — | — | 确认睡觉 | 返回 Todo |
| 设置页 | Todo 底部「设置」 | 上移 | 下移 | 进入设置项 | 返回 Todo |
| 语音包选择 | 设置 → 语音包 | 上移 | 下移 | 确认切换 | 返回设置 |
| 学习统计 | 设置 → 学习统计 | 上下翻页 | 上下翻页 | — | 返回设置 |

### 8.2 三按键操作优化技巧

- **长按 OK = 返回**（全局统一，与现有 demo 一致）
- **双击 OK = 快速完成当前任务**（在 Todo 列表中）
- **长按上/下 = 快速滚动**（任务列表较长时）

---

## 九、睡眠模式与唤醒

### 9.1 睡觉流程

```
点击"睡觉"按钮
    │
    ▼
播放睡前语音 + 显示今日完成总结
    │
    ▼
用户按 OK 确认睡觉
    │
    ▼
保存今日统计数据
    │
    ▼
设置 RTC 闹钟（次日起床时间）
    │
    ▼
关闭背光 → 进入深度休眠
    │
    ▼
次日 RTC 唤醒 → 播放起床音乐 → 显示新的一天
```

### 9.2 功耗估算

| 模式 | 电流 | 续航（520mAh） |
|------|------|---------------|
| 正常使用（亮屏+音频） | ~150mA | ~3.5 小时 |
| 息屏待机（仅 MCU+RTC） | ~20mA | ~26 小时 |
| 深度休眠 | ~10μA | ~数年 |

日常使用（每天亮屏 1-2 小时，其余待机+夜间深睡），预计续航 **3-5 天**。

---

## 十、开发模块与优先级

### Phase 1 — 核心功能（先做）

| 模块 | 内容 | 预估工作量 |
|------|------|-----------|
| 1. 项目脚手架 | 新建 `app_study.c` + 注册到主菜单 | 0.5 天 |
| 2. 任务数据层 | NVS 读写任务、增删改查接口 | 1 天 |
| 3. Todo 首页 UI | 任务列表展示、滚动、选中态 | 1.5 天 |
| 4. 任务添加/编辑 | 模板选择 + 类别选择 + 时间设置 | 1.5 天 |
| 5. 提醒调度器 | FreeRTOS 定时器 + 到点弹窗 + RTTTL 提示音 | 1.5 天 |
| 6. 日常秩序场景 | 预设场景 + 完成触发语音 + 睡觉流程 | 1.5 天 |
| 7. ADPCM 语音播放 | 解码器 + SPIFFS 音频读取 + 播放接口 | 1 天 |
| 8. 水豚噜噜语音包 | 录制/生成所有语音，打包成 ADPCM | 1 天 |

### Phase 2 — 进阶功能

| 模块 | 内容 | 预估工作量 |
|------|------|-----------|
| 9. 语音输入（录音） | 录音功能 + VAD 静音检测 | 1 天 |
| 10. BLE STT 传输 | BLE GATT 服务 + 手机端协议 | 2 天 |
| 11. 语音包管理 | 语音包列表 + 切换 + BLE 下载更新 | 2 天 |
| 12. 学习统计 | 数据统计 + 统计展示页 | 1 天 |
| 13. 深度睡眠 | 睡觉模式 + RTC 唤醒 | 1 天 |
| 14. 低功耗优化 | 息屏、降频、电量优化 | 1 天 |

### Phase 3 — 锦上添花

| 模块 | 内容 |
|------|------|
| 15. 专注模式 | 番茄钟 + 屏蔽非紧急提醒 |
| 16. 成就系统 | 连续打卡徽章、科目进度勋章 |
| 17. 更多语音包 | 其他动画角色语音（需要素材） |
| 18. 自定义完成文案 | 用户可自己录鼓励语 |

---

## 十一、与现有代码的集成方式

### 11.1 文件结构

```
main/
├── app_study/               # 新增：考研助手模块
│   ├── app_study.c          # 主入口（注册到 DEMOS）
│   ├── app_study.h
│   ├── study_ui.c           # UI 相关（所有页面）
│   ├── study_ui.h
│   ├── study_task.c         # 任务数据管理
│   ├── study_task.h
│   ├── study_scheduler.c    # 提醒调度器
│   ├── study_scheduler.h
│   ├── study_voice.c        # 语音播放 + ADPCM 解码
│   ├── study_voice.h
│   ├── study_category.c     # 类别定义 + 完成文案
│   ├── study_category.h
│   └── study_audio_assets.h  # RTTTL 旋律定义
```

### 11.2 注册到主菜单

在 `main.c` 的 `DEMOS[]` 中新增一项：
```c
{ "Study", app_study_enter, app_study_exit, app_study_key },
```

### 11.3 复用现有组件

| 现有组件 | 用途 |
|---------|------|
| `bsp_display.h` + LVGL | 显示 UI |
| `bsp_button.h` | 三按键输入 |
| `bsp_audio.h` | I2S 音频播放/录音 |
| `bsp_battery.h` | 电量显示 |
| `ui_pixel.h` | 像素风 UI 组件（面板、标签、吉祥物） |
| `demo_ble.c` 参考 | BLE 通信（语音传输用） |
| `demo_low_power.c` 参考 | 低功耗 / 深睡模式 |

---

## 十二、待确认问题

在开始开发前，请确认以下几点：

1. **语音输入方案**：是否接受"手机端 STT"方案？还是希望纯离线使用（那就只用模板选字）？
2. **语音包来源**：水豚噜噜的语音是你有现成素材，还是需要用 TTS 生成？如果需要生成，推荐用哪个 TTS 引擎？
3. **初始任务模板**：要不要预设一些常用任务模板？比如"背单词""做高数题""复习政治"等。
4. **睡觉时间**：默认起床/睡觉时间设几点合适？（比如 7:00 / 23:00）
5. **是否需要专注模式（番茄钟）**：在 Phase 2 中，这个优先级高吗？
6. **数据同步**：需不需要和手机同步任务数据？还是通行证本地独立使用？

---

*方案版本 v1.0 · 基于 ai-passport 主分支设计*
